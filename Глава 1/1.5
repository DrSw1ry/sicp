#lang racket

define (p) (p))

(define (test x y)
  (if (= x 0)
      0
      y))

(test 0 (p))
;при использовании аппликативного порядка вычислений процедура
;не выполнится, т.к не будет возможности вычислить процедуру P  
;при нормальном порядке все будет нормально